let createApp;_21e‍.w("./server/server",[["createApp",["createApp"],function(v){createApp=v}]]);require("dotenv").config()


const app = createApp();

_21e‍.g.console.log(process.env.PGPORT, 'PGPORTVAR')

if (NODE_ENV === "production") {
  app.listen(PORT, IP, () => {
    console.log(`server is up and listening on ${IP}:${PORT}`);
  });
} else {
  app.listen(3001, () => {
    console.log(`server is up and listening on port 3001`);
  });
}
